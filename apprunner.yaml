version: 1.0
runtime: python3
build:
  commands:
    - pip install -r gestion_carteras_api/requirements.txt
run:
  command: uvicorn gestion_carteras_api.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
health-check:
  path: /

